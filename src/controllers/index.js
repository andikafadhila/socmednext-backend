const authControllers = require("./authControllers");
const postsControllers = require("./postsControllers");
const editControllers = require("./editControllers");

module.exports = {
  authControllers,
  postsControllers,
  editControllers,
};
